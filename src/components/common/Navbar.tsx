import { HamburgerIcon } from "@chakra-ui/icons";
import { HStack } from "@chakra-ui/react";

export const Navbar = () => {
  return (
    <HStack>
      <HamburgerIcon w={12} h={12} />
    </HStack>
  );
};
